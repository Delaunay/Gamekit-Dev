from setuptools import setup

setup(name='gamkekit',
      version='0.0.0',
      description='Extend ue4ml for easier interactions',
      author='Pierre Delaunay',
      author_email='pierre@delaunay.io',
      license='',
      packages='gamekit',
      zip_safe=True,
      install_requires=['ue4ml'],
      python_requires='>=3.6.*',
)
